name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry + build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Package
        shell: bash
        run: |
          set -euo pipefail
          bin=auditui
          staging=auditui-${{ github.ref_name }}-${{ matrix.target }}
          mkdir -p "$staging"
          cp "target/${{ matrix.target }}/release/$bin" "$staging/"
          cp README.md LICENSE "$staging/"
          # Bundle the session-audit skill so install.sh can copy it from
          # the extracted tarball — avoids a second network round-trip
          # against raw.githubusercontent.com, which is blocked on many
          # corporate networks.
          if [ -f skills/session-audit/SKILL.md ]; then
            mkdir -p "$staging/skills/session-audit"
            cp skills/session-audit/SKILL.md "$staging/skills/session-audit/SKILL.md"
          fi
          tar czf "$staging.tar.gz" "$staging"
          shasum -a 256 "$staging.tar.gz" > "$staging.tar.gz.sha256"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: auditui-${{ matrix.target }}
          path: |
            auditui-*-${{ matrix.target }}.tar.gz
            auditui-*-${{ matrix.target }}.tar.gz.sha256

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      # Need source checkout so install.sh can be uploaded as a release
      # asset (the bootstrap one-liner pulls it from
      # github.com/<repo>/releases/latest/download/install.sh).
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Stage install.sh
        run: cp install.sh artifacts/install.sh

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/*.tar.gz
            artifacts/*.sha256
            artifacts/install.sh
          draft: false
          generate_release_notes: true
